spring.application.name=revplaydemo
server.port=8080

# MySQL datasource
spring.datasource.url=jdbc:mysql://localhost:3306/revplay_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username= root
spring.datasource.password= Jayavardhan@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Optional OpenAPI paths
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html


app.security.jwt.secret=${JWT_SECRET:replace-with-at-least-32-characters-secret}
app.security.jwt.access-token-expiration-seconds=${JWT_ACCESS_EXPIRY_SECONDS:3600}
app.security.jwt.refresh-token-expiration-seconds=${JWT_REFRESH_EXPIRY_SECONDS:1209600}
